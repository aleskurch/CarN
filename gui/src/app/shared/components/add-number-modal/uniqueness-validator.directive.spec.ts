import { UniquenessValidator } from './uniqueness-validator.directive';

describe('UniquenessValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniquenessValidator();
    expect(directive).toBeTruthy();
  });
});
